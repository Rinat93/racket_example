#!/usr/bin/racket
#lang racket
(provide database)
(define database #hash(("Server" . localhost) 
                        ("user" . rinat) 
                        ("pass" . 930114)
                        ("port" . 5043)
                        ("database" . postgresql)))
